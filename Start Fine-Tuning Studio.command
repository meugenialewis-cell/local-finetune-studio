#!/bin/bash
# Double-click this file to set up and start Local Fine-Tuning Studio.
# All the real work happens in setup.sh — this is just the clickable wrapper.
cd "$(dirname "$0")"
exec bash ./setup.sh
